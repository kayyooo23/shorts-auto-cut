# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Sidecar-бинарники (backend .exe + ffmpeg/ffprobe, ~330МБ суммарно) —
# собираются/копируются отдельно, см. ../../backend/scripts/build_exe.ps1.
# Не хранятся в git.
/binaries/
